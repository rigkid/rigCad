# cad hero

Orchestration smoke for **rigCad**: seeds Code Editor; `ready()` false until embeds.

```bash
cmake -S . -B build
cmake --build build
powershell -File ./capture-preview.ps1
```

See [pack README](../../README.md).
